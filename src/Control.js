import React, { PureComponent } from 'react';

class Control extends PureComponent {
    allLightsOff() {
        /* User clicked the allLightsOff button.
         * Consider if this applies to your component and either
         * override this method, or don't
         */
    }

    allBlindsUp() {
        /* User clicked the allBlindsUp button.
         * Consider if this applies to your component and either
         * override this method, or don't
         */
    }

    allBlindsDown() {
        /* User clicked the allBlindsDown button.
         * Consider if this applies to your component and either
         * override this method, or don't
         */
    }
}

export default Control;
